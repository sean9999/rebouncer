{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "run on ./tmp/**",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/main.go",
            "args": ["--dir", "${workspaceFolder}/tmp"]
        }
    ]
}
