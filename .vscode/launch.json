{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "run example inotify command",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/rebounce/main.go",
            "cwd": "${workspaceFolder}"
        }
    ]
}
