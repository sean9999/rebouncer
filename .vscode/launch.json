{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Binary",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/main.go"
        }
    ]
}
